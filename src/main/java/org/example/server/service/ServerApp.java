package org.example.server.service;

import org.example.server.utils.ServerRunner;

public class ServerApp {
    public static void main(String[] args) {
            new ServerRunner().runServer();
    }
}
